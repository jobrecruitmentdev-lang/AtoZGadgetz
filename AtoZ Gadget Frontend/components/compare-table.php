<?php
declare(strict_types=1);
?>
<!-- Side-by-Side Compare Dynamic Table Matrix -->
<div class="compare-table-wrapper" style="overflow-x: auto; width: 100%; border: 1px solid var(--border); border-radius: var(--radius-md); background-color: var(--surface);">
  <table class="compare-matrix-table" style="width: 100%; border-collapse: collapse; min-width: 600px; text-align: left; font-size: var(--text-sm);">
    
    <!-- Table head (Images + remove buttons) -->
    <thead id="compare-table-header" style="background-color: var(--background-alt); border-bottom: 2px solid var(--border);">
      <!-- Loaded dynamically via compare.js -->
    </thead>

    <!-- Table body (Attributes comparisons rows) -->
    <tbody id="compare-table-body" style="color: var(--text);">
      <!-- Loaded dynamically via compare.js -->
    </tbody>
    
  </table>
</div>
